/*
Deployment script for SchedulerCore

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "SchedulerCore"
:setvar DefaultFilePrefix "SchedulerCore"
:setvar DefaultDataPath ""
:setvar DefaultLogPath ""

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[Schedule].[UserId] on table [dbo].[Schedule] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Schedule])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Altering [dbo].[Schedule]...';


GO
ALTER TABLE [dbo].[Schedule]
    ADD [UserId] INT NOT NULL;


GO
PRINT N'Creating [dbo].[UserSocial]...';


GO
CREATE TABLE [dbo].[UserSocial] (
    [Id]            INT            IDENTITY (1, 1) NOT NULL,
    [Userid]        INT            NOT NULL,
    [SocialId]      INT            NOT NULL,
    [SocialAccount] NVARCHAR (MAX) NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);


GO
PRINT N'Creating [dbo].[FK_UserSocial_ToUser]...';


GO
ALTER TABLE [dbo].[UserSocial] WITH NOCHECK
    ADD CONSTRAINT [FK_UserSocial_ToUser] FOREIGN KEY ([Userid]) REFERENCES [dbo].[User] ([Id]);


GO
PRINT N'Creating [dbo].[FK_Schedule_ToUser]...';


GO
ALTER TABLE [dbo].[Schedule] WITH NOCHECK
    ADD CONSTRAINT [FK_Schedule_ToUser] FOREIGN KEY ([UserId]) REFERENCES [dbo].[User] ([Id]);


GO
